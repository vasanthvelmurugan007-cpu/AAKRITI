# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=8000
# For production, set to your domain (e.g., https://yourdomain.com)
# For local development, use http://localhost:3000
VITE_API_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Option 1: MySQL/TiDB Cloud (Recommended for Production)
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://user:password@host:3306/dbname

# Option 2: SQLite (Development Only - Not recommended for production)
# Leave DATABASE_URL empty to use SQLite (will auto-create server/database.sqlite)
# DATABASE_URL=

# Optional: Custom SQLite database path (only used if DATABASE_URL is not set or is SQLite)
# SQLITE_DB_PATH=server/database.sqlite


# -----------------------------------------------------------------------------
# Cloudinary Configuration (Required for Image Uploads)
# Get these from: https://cloudinary.com/console
# -----------------------------------------------------------------------------
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# -----------------------------------------------------------------------------
# File Storage Paths (Optional - defaults provided)
# -----------------------------------------------------------------------------
# Directory for uploaded files (if not using Cloudinary exclusively)
UPLOAD_DIR=server/uploads
# Directory for thumbnail cache
THUMB_DIR=server/thumbnails
# Directory containing the built frontend (React dist folder)
CLIENT_BUILD_PATH=dist

# -----------------------------------------------------------------------------
# Security Configuration (Optional but Recommended)
# -----------------------------------------------------------------------------
# Secret key for JWT tokens (generate with: openssl rand -hex 32)
# SECRET_KEY=your-secret-key-here
# Allowed CORS origins (comma-separated for multiple domains)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# -----------------------------------------------------------------------------
# Environment Type
# -----------------------------------------------------------------------------
# Set to 'production' when deploying
ENVIRONMENT=development
